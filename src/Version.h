/* Version.h — Single source of truth for WinDV version numbers.
 *
 * Included by WinDV.rc (resource compiler) and can be included
 * by .cpp files if needed at runtime.
 *
 * To release a new version, change ONLY this file.
 */

#define VER_MAJOR       1
#define VER_MINOR       6
#define VER_PATCH       0
#define VER_BUILD       0

#define VER_STRING      "1.6.0"
#define VER_APP_TITLE   "WinDV 1.6.0"
